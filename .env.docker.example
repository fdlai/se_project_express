# Copy this file to .env.docker.production (or .env.docker.local)
# and replace the values with real ones.

# Generate a long random string for JWT_SECRET (e.g., 32+ characters)
# Example: use a password generator or `openssl rand -hex 32`

JWT_SECRET=replace_with_random_string
PORT=3001